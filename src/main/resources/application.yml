server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://localhost/users_service
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
